import { useEffect,useState } from "react";
import { AlertTriangle,Clock3 } from "lucide-react";
import { COLORS } from "../../constants/theme";
import { supabase } from "../../services/supabase";
const TABLES=["tasks","calendar_events","attachments","quick_notes","important_dates","finance_accounts","finance_cards","finance_categories","finance_recurrences","finance_transactions","finance_budgets","finance_goals","finance_investments","finance_subscriptions","finance_goal_contributions","finance_investment_movements","finance_investment_snapshots","house_shopping_items","house_maintenances","house_assets","pets","pet_weights","pet_vaccinations","pet_deworming","pet_health_plans","pet_appointments","pet_medications","projects","project_milestones","project_notes","project_decisions","project_risks","work_demands","inbox_items","habits","house_inventory_items","goals","lists","documents","journal_entries","study_courses","study_items","study_sessions","study_materials","content_pillars","content_items","content_metrics","entertainment_items","entertainment_progress"];
export default function TrashExpirationNotice(){const[count,setCount]=useState(0),[loading,setLoading]=useState(true);useEffect(()=>{load()},[]);async function load(){setLoading(true);const limit=new Date(Date.now()-30*86400000).toISOString();const results=await Promise.all(TABLES.map(async table=>{const{count,error}=await supabase.from(table).select("id",{count:"exact",head:true}).not("deleted_at","is",null).lte("deleted_at",limit);return error?0:count||0}));setCount(results.reduce((a,b)=>a+b,0));setLoading(false)}return <section style={{background:count?COLORS.warningLight:COLORS.primaryLight,border:`1px solid ${count?COLORS.warning:COLORS.border}`,borderRadius:12,padding:11,marginBottom:14}}><div style={{display:"flex",alignItems:"flex-start",gap:9}}>{count?<AlertTriangle size={16} color={COLORS.warning}/>:<Clock3 size={19} color={COLORS.primaryDark}/>}<div><strong style={{color:COLORS.ink,fontSize:13}}>{loading?"Verificando prazos...":count?`${count} item${count===1?"":"s"} há mais de 30 dias na Lixeira`:"Nenhum item expirado"}</strong><div style={{color:COLORS.inkSoft,fontSize:11,lineHeight:1.45,marginTop:4}}>{count?"Revise os itens abaixo e use o botão vermelho para excluir definitivamente. Nada será apagado sem sua confirmação.":"Os itens permanecem recuperáveis. Após 30 dias, serão sinalizados para você decidir."}</div></div></div></section>}
